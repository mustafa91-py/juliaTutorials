# bazı veri tipleri string, char, integer, float

#veri tiplerini koleksiyon haline (collection) getirdiğimize  veri yapısını (Data Structure) oluşturur ve aynı zamanda bir veri tipi (data type) sayılır

col_vector = [1, 2, 3]

typeof(col_vector) # bu bir sütun yani 1 boyutlu vektördür kullanılan ad ise array dir

row_vector = Float32[1.1 2.2 3.3]# aralara virgül koymadan boşluk bırakarak yazdığımız değerler 1 x 3 şeklideki 2 boyutlu bir matriks dir
# 1 x 3 = 1 satır sayısı (1.boyut), 3 sütun sayısı (bunla beraber 2.boyut) 2 boyutlu array yani matrix dir

typeof(row_vector)